def count_spaces(userinput):
    spaces = 0
    for char in userinput:
        if char == " ":
            spaces += 1
    return spaces

def main():
    userinput = input("Please enter a phrase:")
    print("There are",count_spaces(userinput),"spaces in your phrase")

main()
